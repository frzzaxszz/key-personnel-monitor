# 重点人员综合管控平台 — 项目整体说明文档

> 版本：v1.0　|　说明覆盖后端 FastAPI、前端 Vue3、MySQL/SQLite 数据存储与一键部署。

---

## 1. 项目简介

面向基层治理场景的**重点人员综合管控数据大屏**，以中国地图为视觉中心，聚合展示重点人员总数、风险等级、管控情况等 KPI，并支持**图表联动、区域钻取、Excel 导入、MySQL 数据源接入与自动更新**。

核心价值：让用户通过"一块大屏"实时掌握重点人员分布与风险态势，无需逐条翻阅台账。

---

## 2. 技术架构

| 层 | 技术 | 说明 |
| --- | --- | --- |
| 前端 | Vue 3 + Vite + ECharts | Singe Page，深色大屏风，hash 路由 |
| 前端 UI | 自定义组件 `BaseChart` | 统一封装 ECharts 图表实例 |
| 后端 | Python + FastAPI | REST 接口 + 后台实时任务 |
| ORM | SQLAlchemy 2.0 | 运行时切换 SQLite / MySQL |
| 数据导入 | openpyxl | Excel 按表头导入/导出 |
| 数据库 | MySQL（默认本地）/ SQLite（兜底） | 配置持久化在 `db_config.json` |

启动后访问：
- 前端：`http://127.0.0.1:5173`
- 后端：`http://127.0.0.1:5174`
- 健康检查：`http://127.0.0.1:5174/api/health`

---

## 3. 功能清单与页面

| 路由 | 页面 | 主要功能 |
| --- | --- | --- |
| `/dashboard` | 数据大屏 | KPI 指标、中央地图、多图表联动钻取、实时动态流 |
| `/persons` | 人员管理 | 列表筛选/分页、搜索、新增编辑删除、照片上传 |
| `/persons/:id` | 人员详情 | 详情、走访记录查看/登记 |
| `/import` | 数据导入 | Excel 导入（缺失字段兜底）、模板下载、导入日志 |
| `/settings` | 系统设置 | MySQL 连接配置、自动更新开关、重建/清空模拟数据 |

---

## 4. 数据大屏设计

### 4.1 布局
```
┌─────────────────────────────────────────────────────────┐
│  顶栏：Logo / 标题 / 右侧导航（大屏·人员·导入·设置）        │
├─────────────────────────────────────────────────────────┤
│ ☆指标栏（KPI）                                          │
│  重点人员总数·高风险人员·今日新增·在控率·待处置预警           │
├──────────┬───────────────────────────────┬──────────────┤
│ 左栏      │       中栏（地图为主）             │  右栏       │
│ 人员类别构成│   [筛选条:日期范围+钻取清除]        │ 近7日新增/处置│
│ 地区TOP   │   中国地图(热力着色+风险点)          │ 近7日预警趋势 │
│ 管控状态  │   下方3图：风险等级·近6月·预警类型     │             │
│          │   ────────────────────────────  │             │
│          │   底部：实时预警滚动 + 访问处置动态     │             │
└──────────┴───────────────────────────────┴──────────────┘
```
> 地图左上角的**筛选条常驻**：左侧为"入库日期"起止日期选择（可自选日期范围），当存在钻取/类别/风险等过滤时，右侧动态显示"当前筛选"chips，可逐个清除或点"重置"。

### 4.2 地图（重点）
- 采用**单一 `geo` 图层**承载两件事：按省份聚合值做**区域色阶填充**（热力）+ 按人员坐标做**风险散点/光效**。二者共用同一 geo，滚轮/拖动时**始终重合**，杜绝"双图层缩放错位"。
- 颜色规则：按省人数相对最大值分档（高/中/低 → 红/橙/青）。
- **钻取**：点击省份 → 以省会坐标为中心放大（zoom 约 2.6），同时所有图表过滤至该省；地图左上角浮出"当前筛选：××　×返回全国"条。
- 风险点大小：高风险大 / 中 / 低递减。

### 4.3 联动与钻取
所有图表点击都通过前端 `filters` 状态 + `refreshAll()` 一次性把过滤参数传给后端，实现**双向联动**：

| 图表 | 点击行为 |
| --- | --- |
| 地图（省份） | 钻取至该省并全局过滤 |
| 人员类别构成（饼） | 按类别过滤人员 |
| 地区人员分布 TOP（柱） | 按省份过滤 |
| 管控状态分布（饼） | 按管控状态过滤 |
| 风险等级占比（饼） | 按风险等级过滤（`risk` 参数）|
| 预警类型分布（饼） | 按预警类型过滤预警链路（`alert_type`）|
| 近6月新增趋势 | 点击某月 → 过滤该月新增人员（`month` 参数）|
| 近7日预警趋势 | 点击某日 → 过滤当日有预警的人员（`alert_date`=该日 full_date）|

**入库日期范围筛选**：地图筛选条上可自选"入库日期"起止（绑定后端 `date_from / date_to`），对人员 `created_at` 做 `>= 起 / < 止次日` 过滤，同样作用到 KPI、全部图表与地图打点，并与其它过滤维度叠加联动。

后端 `/api/dashboard/summary` 与 `/map` 均支持过滤参数：`district / category / status / risk / month / date_from / date_to`（过滤人员）、`alert_type / alert_date`（过滤预警并联动人员）。

### 4.4 指标栏优化
- 今日新增卡：显示 `较昨日 ↑/↓ +N`（增红减绿）
- 待处置预警卡：显示 `今日预警 ↑/↓` 环比，并列明今日走访次数
- 字段来自后端 `yesterday_new / today_alerts / yesterday_alerts`

### 4.5 图表标签
除地图与地区 TOP 外，**人员类别构成、风险等级占比、管控状态分布、预警类型分布、近6月新增趋势、近7日新增/处置趋势、近7日预警趋势**均在图上直接显示 `名称/数量` 标签，无需鼠标悬浮（悬浮用于查看百分比）。

### 4.6 实时动态流
- 底部滚动条展示**最新预警 + 最新走访动态**，每 15 秒自动刷新。
- 预警与动态中的**人员姓名可直接点击**，弹出人员详情浮窗（含照片、类别、风险、预警内容等），并可通过"查看完整档案"跳转至 `/persons/:id`。

### 4.7 人员完整档案居中
`/persons/:id` 详情页（完整档案）的内容区 `.detail-wrap` 设置了 `max-width: 1080px; margin: 0 auto`，在宽屏下水平居中显示。

---

## 5. 数据模型

| 表 | 说明 | 关键字段 |
| --- | --- | --- |
| `persons` | 重点人员 | name、id_card(唯一)、gender、age、category、risk_level(高/中/低)、control_status(在控/待核查/脱管/在控就医)、district、street、address、longitude/latitude、manager、photo、notes |
| `visit_records` | 走访/处置 | person_id、visit_date、content、manager |
| `alert_records` | 预警 | person_id、alert_type、alert_level、content、district、status(待处置/已处置)、created_at |
| `import_logs` | 导入日志 | filename、total、success、failed、errors(JSON) |
| `db_settings` | 数据库配置+开关 | db_type、host、port、database、username、password、auto_sync、sync_interval |

---

## 6. 数据存储：SQLite 与 MySQL

- **默认 MySQL**：连接信息在 `backend/data/db_config.json`（`db_type=mysql`），启动即用；MySQL 不可达时兜底 SQLite。
- **MySQL 接入/切换**：《系统设置》中选择 MySQL，填写 host/port/库名/用户名/密码，点"测试连接"。
  - 后端 `ensure_mysql_database` 在库不存在时自动 `CREATE DATABASE`（utf8mb4）
  - 成功后全局引擎切换至 MySQL 并建表
  - 配置写入 `backend/data/db_config.json`，**重启后自动沿用**，无需重复配置
- 切换回 SQLite：把配置改成 sqlite 并测试即可。

---

## 7. 模拟数据与自动更新

- **种子数据**：启动时生成 **5000 条**重点人员，覆盖全国 31 省（省会在 `seed.py` 预置坐标，打点随机会小抖动避免重叠）。联合生成约 300 人×走访记录和 80–130 条预警。
- **分布逻辑**：涉毒/肇事肇祸/涉邪教倾向高风险；管控状态按 在控 78%/待核查 12%/脱管 6%/在控就医 4% 加权。
- **自动更新**：`sync_service.simulate_realtime` 按后台任务节奏（`_realtime_loop`）生成新的预警或走访动态，让大屏"动"起来；间隔由 `settings.auto_sync + sync_interval` 控制。
- **重置**：
  - `POST /api/settings/reseed?count=5000`：清空全部业务数据并重建
  - `POST /api/settings/drop`：仅清空业务数据，保留表结构与设置

---

## 8. Excel 数据导入（写入数据库）

后端 `import_export.py`，`POST /api/import/excel`：

1. **按表头名称映射列**（非固定列序）。支持表头：
   `姓名、身份证号、性别、年龄、手机号、人员类别、风险等级、管控状态、所属区域、街道、详细地址、责任民警、备注`
2. **必填校验**：`姓名`、`身份证号` 必须存在，否则接口返回 400 并提示缺失列。
3. **缺失可选字段兜底**：缺少的选填列自动填写默认值（如 性别=男、年龄=0、风险等级=低、管控状态=在控、人员类别=涉稳人员…），并在返回的 `warnings` 中提示已用默认值。
4. **幂等/去重**：身份证号已存在计为失败行。
5. 结果返回 `{ total, success, failed, errors[], warnings[] }`；每次导入写入 `import_logs`，可在导入页查看历史。

- 模板下载：`GET /api/import/template`
- 全部导出：`GET /api/import/export`
- 导入日志：`GET /api/import/logs`

---

## 9. 后端 API 一览

前端通过 Vite 代理 `/api` 至 5174 端口。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/health` | 健康检查 |
| GET | `/api/dashboard/summary` | 全部指标与图表聚合（支持 `district/category/status/risk/month/date_from/date_to` 过滤、`alert_type/alert_date` 联动预警）|
| GET | `/api/dashboard/map` | 地图按省聚合 + 打点（支持 `district/category/status/risk/month/date_from/date_to/alert_type`）|
| GET/POST | `/api/persons` | 人员列表（筛选/分页）/ 新增 |
| GET | `/api/persons/options` | 筛选项常量 + 区域列表 |
| GET/PUT/DELETE | `/api/persons/{id}` | 详情 / 更新 / 删除 |
| POST | `/api/persons/{id}/photo` | 上传照片 |
| GET/POST | `/api/persons/{id}/visits` | 走访记录查询 / 新增 |
| GET | `/api/import/template` | 下载导入模板 |
| POST | `/api/import/excel` | 导入 Excel |
| GET | `/api/import/export` | 导出 Excel |
| GET | `/api/import/logs` | 导入日志 |
| GET/PUT | `/api/settings/db` | 数据库配置读取 / 更新 |
| POST | `/api/settings/db/test` | 测试连接切换引擎 |
| POST | `/api/settings/reseed` | 重建模拟数据 |
| POST | `/api/settings/drop` | 清空业务数据 |

---

## 10. 部署与启动

### 10.1 一键启动（推荐）
双击根目录 **`启动系统.bat`**：
1. 启动后端 `uvicorn main:app`（127.0.0.1:5174）
2. 启动前端 Vite（127.0.0.1:5173）
3. 自动打开浏览器 `http://127.0.0.1:5173`

### 10.2 手动启动
```bash
# 后端
cd backend
.venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 5174

# 前端（另开终端）
cd frontend
npm run dev
```

> **重要**：后端 `uvicorn` 未开启 `--reload`，修改后端代码后需手动重启后端。

### 10.3 依赖安装
- 后端：`cd backend && .venv\Scripts\pip install -r requirements.txt`
- 前端：`cd frontend && npm install`

### 10.4 环境要点
- 后端依赖：`fastapi / uvicorn[standard] / sqlalchemy / pydantic / openpyxl / python-multipart / pymysql / cryptography`
- 前端依赖：`vue / vue-router / axios / echarts / vite`
- Python 版本 3.7+ 即可（当前 `.venv` 为 3.14）

---

## 11. 常见问题（FAQ）

1. **后端改代码不生效？**
   无 `--reload`，改完必须重启后端进程。

2. **地图图层错位？**
   确认热力与打点挂在同一个 `geo` 上（`mapOption` 里不另设独立 `map` series）。

3. **Excel 导入提示缺少列？**
   模板必填列为"姓名、身份证号"；其余缺失走默认值并在 `warnings` 提示。

4. **连不上 MySQL？**
   核对 host/端口/账号/密码；库不存在会自动创建；可在设置页"测试连接"看具体报错。

5. **切换数据库后数据在旧库里？**
   SQLite 与 MySQL 是两套存储；切换后需在新库重新 reseed 或导入。

6. **今日新增/预警显示"持平"？**
   环比依赖后端 `yesterday_new/today_alerts/yesterday_alerts`，确认后端已用最新代码重启。

---

## 12. 目录结构总览

```
e:\大屏\
├─ 启动系统.bat
├─ AGENTS.md
├─ backend\
│  ├─ main.py / config.py / database.py / models.py
│  ├─ schemas.py / seed.py / sync_service.py / requirements.txt
│  ├─ data\            (app.db、uploads、db_config.json)
│  └─ routers\
│     ├─ dashboard.py / persons.py / import_export.py / settings.py
└─ frontend\
   ├─ package.json
   └─ src\
      ├─ main.js / App.vue
      ├─ api\index.js / router\index.js
      ├─ assets\geo\china.json
      ├─ components\BaseChart.vue / styles\main.css
      └─ views\Dashboard / PersonList / PersonDetail / ImportData / Settings
```